import { observable, computed } from "mobx"
import Base from "./Base"
import { Store } from "../Store"

export default class Bobo extends Base {
    @observable
    state = {

    }

    actions = {
        action1: async () => {

        }
    }
}
